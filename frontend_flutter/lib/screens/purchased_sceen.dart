import 'package:flutter/material.dart';

class PurchasedSceen extends StatelessWidget {
  const PurchasedSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
